version: '3.8'

services:
  test-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: bookstore-test-db
    environment:
      ACCEPT_EULA: "Y"
      MSSQL_SA_PASSWORD: "SecretPassword123!"
    ports:
      - "1433:1433"